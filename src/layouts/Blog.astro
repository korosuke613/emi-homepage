---
import { Box, Grid } from "@mui/joy";
import { Typography } from "../components/Typography";
import type { BlogSchema } from "../utils/microcms";
import Layout from "./Layout.astro";

type Props = {
  blog: BlogSchema;
};

const { blog } = Astro.props;
---

<Layout title={blog.title} description={""} lang={blog.language[0]}>
  <Grid container sx={{
    padding: "2rem",
    maxWidth: "800px",
    margin: "0 auto",
    figure: {
      margin: "0",
      textAlign: "center",
    },
    img: {
      width: "60%",
      height: "auto",
    },
  }}>
  <Typography>{blog.createdAt}</Typography>
  <Box set:html={blog.content}/>      
  </Grid>
</Layout>
