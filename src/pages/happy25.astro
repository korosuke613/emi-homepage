---
import { AspectRatio, Box, Card, CardContent, CardCover, Grid } from "@mui/joy";
import { PageCover } from "../components/PageCover";
import { Typography } from "../components/Typography";
import type { Languages } from "../i18n/ui";
import {
  useLocalTranslations,
  useLocalTranslationsWithElement,
  useSharedTranslations,
} from "../i18n/utils";
import Layout from "../layouts/Layout.astro";

interface Props {
  lang: Languages;
}

const props = Astro.props;

const lang = props.lang ?? "ja";
const t = useSharedTranslations(lang);
const tl = useLocalTranslations(lang);
const TLE = useLocalTranslationsWithElement(lang);
---

<Layout title={tl({
	ja: "牧浦えみ！祝 25 歳！！",
	en: "Emi Makura! Happy 25th Birthday!!",
})} description={t('description')} lang={lang}>
	<Box 
		sx={{ 
			height: "10rem",    
			textAlign: "center",
			justifyContent: "center" 
		}}
	>	
		<Grid container spacing={2} sx={{ flexGrow: 1 }}>
			<Grid xs={12}>
				<Box
					component="img"
					alt=""
					src="/happy25.jpg"
					width="100%"
					maxWidth={1200}
					maxHeight={555}
					sx={{
						borderRadius: "0.5rem",
					}}
				/>
			</Grid>

			<Grid xs={12}>
				<Typography level="h4">
					{tl({
						ja: "めでたいねぇ",
						en: "Congratulations!",
					})}
				</Typography>
			</Grid>
		</Grid>
	</Box>
 </Layout>
