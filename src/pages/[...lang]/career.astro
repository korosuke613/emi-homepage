---
import { defaultLang, languageKeys } from "../../i18n/ui";
import DefaultCareer from "../career.astro";

export async function getStaticPaths() {
  return languageKeys
    .map((lang) => {
      if (lang === defaultLang) {
        return undefined;
      }
      return { params: { lang } };
    })
    .filter((v) => v !== undefined);
}

const { lang } = Astro.params;
---

<DefaultCareer lang={lang}></DefaultCareer>
