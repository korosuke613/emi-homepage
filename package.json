{
  "name": "non-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "check": "turbo check",
    "check:fix": "turbo check:fix"
  },
  "devDependencies": {
    "turbo": "2.0.6"
  },
  "packageManager": "npm@10.5.0",
  "engines": {
    "node": ">=18"
  }
}
